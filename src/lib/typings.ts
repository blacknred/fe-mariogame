export enum Keys {
  down = "ArrowDown",
  up = "ArrowUp",
  left = "ArrowLeft",
  right = "ArrowRight",
  enter = "Enter",
  space = " ",
}

export type Axes = {
  x: number;
  y: number;
};

export type Size = {
  w: number;
  h: number;
};
